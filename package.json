{
  "name": "guide-app",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "webapp",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace webapp\" \"npm run dev --workspace server\"",
    "build": "npm run build --workspace webapp && npm run build --workspace server",
    "start": "npm run start --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
